[project]
name = "supercopa-bot"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "requests==2.32.3",
    "curl_cffi==0.7.1",
    "beautifulsoup4==4.12.3",
    "lxml==5.2.2",
    "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
